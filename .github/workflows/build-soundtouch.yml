# ============================================================================
# Build SoundTouch Android Native Library
# ============================================================================

name: Build SoundTouch Native Library

on:
  workflow_dispatch:
    inputs:
      soundtouch_version:
        description: 'SoundTouch version'
        required: true
        default: '2.3.3'
        type: string

jobs:
  build-soundtouch:
    name: Build SoundTouch ${{ inputs.soundtouch_version }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
      
      - name: Setup Android SDK
        uses: android-actions/setup-android@v4
      
      - name: Install NDK
        run: |
          echo "Installing NDK 25.1.8937393..."
          yes | sdkmanager "ndk;25.1.8937393"
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/25.1.8937393" >> $GITHUB_ENV
          echo "NDK_HOME=$ANDROID_HOME/ndk/25.1.8937393" >> $GITHUB_ENV
      
      - name: Download SoundTouch Source
        run: |
          VERSION="${{ inputs.soundtouch_version }}"
          echo "Downloading SoundTouch $VERSION..."
          curl -L -o "soundtouch-$VERSION.tar.gz" "https://www.surina.net/soundtouch/soundtouch-$VERSION.tar.gz"
          
          tar -xzf "soundtouch-$VERSION.tar.gz"
          
          if [ -d "soundtouch" ]; then
            echo "SOUNDTOUCH_DIR=soundtouch" >> $GITHUB_ENV
            echo "Extracted to: soundtouch/"
          else
            echo "ERROR: Could not find extracted directory"
            ls -la
            exit 1
          fi
      
      - name: Setup Android Build Directories
        run: |
          mkdir -p "$SOUNDTOUCH_DIR/source/Android-lib/jni"
          mkdir -p "$SOUNDTOUCH_DIR/source/Android-lib/libs"
          echo "Created Android build directories"
      
      - name: Create Android.mk
        run: |
          cd "$SOUNDTOUCH_DIR/source/Android-lib/jni"
          
          # Path to SoundTouch source from jni directory
          SOUNDTOUCH_SRC="../../SoundTouch"
          
          echo "Scanning for .cpp files in: $SOUNDTOUCH_SRC"
          
          if [ ! -d "$SOUNDTOUCH_SRC" ]; then
            echo "ERROR: SoundTouch source not found at $SOUNDTOUCH_SRC"
            echo "Contents of ../../:"
            ls -la ../../
            exit 1
          fi
          
          # Find all .cpp files
          ALL_CPP=$(find "$SOUNDTOUCH_SRC" -maxdepth 1 -type f -name "*.cpp" | sort)
          
          if [ -z "$ALL_CPP" ]; then
            echo "ERROR: No .cpp files found"
            echo "Contents of $SOUNDTOUCH_SRC:"
            ls -la "$SOUNDTOUCH_SRC/"
            exit 1
          fi
          
          echo "Found .cpp files:"
          echo "$ALL_CPP"
          
          # Build source list (relative paths from jni directory)
          SRC_LIST=""
          for file in $ALL_CPP; do
            # Get filename only (files are in same directory)
            filename=$(basename "$file")
            SRC_LIST="${SRC_LIST} ../../SoundTouch/${filename}"$'\n'
            echo "  Adding: ../../SoundTouch/${filename}"
          done
          
          cat > Android.mk << EOF
          LOCAL_PATH := \$(call my-dir)

          include \$(CLEAR_VARS)
          LOCAL_MODULE := soundtouch_core
          LOCAL_SRC_FILES := \\
          $SRC_LIST
          LOCAL_CFLAGS := -O3 -DNDEBUG -fexceptions -frtti
          LOCAL_CFLAGS += -DANDROID -DSOUNDTOUCH_INTEGER_SAMPLES=1
          LOCAL_CPPFLAGS := -std=c++11
          LOCAL_C_INCLUDES := \$(LOCAL_PATH)/../../SoundTouch
          include \$(BUILD_STATIC_LIBRARY)

          include \$(CLEAR_VARS)
          LOCAL_MODULE := soundtouch
          LOCAL_SRC_FILES := soundtouch-jni.cpp
          LOCAL_STATIC_LIBRARIES := soundtouch_core
          LOCAL_LDLIBS := -llog -lz
          LOCAL_CFLAGS := -O3 -DNDEBUG -fexceptions -frtti
          LOCAL_CFLAGS += -DANDROID -DSOUNDTOUCH_INTEGER_SAMPLES=1
          LOCAL_CPPFLAGS := -std=c++11
          LOCAL_C_INCLUDES := \$(LOCAL_PATH)/../../SoundTouch
          include \$(BUILD_SHARED_LIBRARY)
          EOF
          
          echo "Android.mk created with $(echo "$ALL_CPP" | wc -l) source files"
          cat Android.mk
      
      - name: Create Application.mk
        run: |
          cat > "$SOUNDTOUCH_DIR/source/Android-lib/jni/Application.mk" << 'EOF'
          APP_PLATFORM := android-21
          APP_ABI := armeabi-v7a arm64-v8a x86 x86_64
          APP_STL := c++_shared
          APP_CPPFLAGS := -std=c++11 -frtti -fexceptions
          APP_CFLAGS := -O3 -DNDEBUG
          APP_OPTIM := release
          EOF
          
          echo "Application.mk created"
      
      - name: Create soundtouch-jni.cpp
        run: |
          cat > "$SOUNDTOUCH_DIR/source/Android-lib/jni/soundtouch-jni.cpp" << 'EOF'
          #include <jni.h>
          #include <android/log.h>
          #include <string>
          #include <SoundTouch.h>

          #define LOG_TAG "SoundTouch"
          #define LOGD(...) __android_log_print(ANDROID_LOG_DEBUG, LOG_TAG, __VA_ARGS__)

          using namespace soundtouch;
          static std::string g_lastError = "";

          extern "C" JNIEXPORT jlong JNICALL
          Java_net_surina_soundtouch_SoundTouch_newInstance(JNIEnv* env, jclass clazz) {
              SoundTouch* st = new SoundTouch();
              return reinterpret_cast<jlong>(st);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_deleteInstance(JNIEnv* env, jobject obj, jlong handle) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) delete st;
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_setSampleRate(JNIEnv* env, jobject obj, jlong handle, jint sampleRate) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->setSampleRate(sampleRate);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_setChannels(JNIEnv* env, jobject obj, jlong handle, jint channels) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->setChannels(channels);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_setTempo(JNIEnv* env, jobject obj, jlong handle, jfloat tempo) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->setTempo(tempo);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_setPitchSemiTones(JNIEnv* env, jobject obj, jlong handle, jfloat pitch) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->setPitchSemiTones(pitch);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_setRate(JNIEnv* env, jobject obj, jlong handle, jfloat rate) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->setRate(rate);
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_putSamples(JNIEnv* env, jobject obj, jlong handle, jshortArray samples, jint numSamples) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (!st) return;
              jshort* arr = env->GetShortArrayElements(samples, nullptr);
              if (arr) {
                  st->putSamples((SAMPLETYPE*)arr, numSamples);
                  env->ReleaseShortArrayElements(samples, arr, JNI_ABORT);
              }
          }

          extern "C" JNIEXPORT jint JNICALL
          Java_net_surina_soundtouch_SoundTouch_receiveSamples(JNIEnv* env, jobject obj, jlong handle, jshortArray buffer, jint maxSamples) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (!st) return 0;
              jshort* arr = env->GetShortArrayElements(buffer, nullptr);
              if (!arr) return 0;
              uint received = st->receiveSamples((SAMPLETYPE*)arr, maxSamples);
              env->ReleaseShortArrayElements(buffer, arr, 0);
              return received;
          }

          extern "C" JNIEXPORT jint JNICALL
          Java_net_surina_soundtouch_SoundTouch_numSamples(JNIEnv* env, jobject obj, jlong handle) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              return st ? st->numSamples() : 0;
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_clear(JNIEnv* env, jobject obj, jlong handle) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->clear();
          }

          extern "C" JNIEXPORT void JNICALL
          Java_net_surina_soundtouch_SoundTouch_flush(JNIEnv* env, jobject obj, jlong handle) {
              SoundTouch* st = reinterpret_cast<SoundTouch*>(handle);
              if (st) st->flush();
          }

          extern "C" JNIEXPORT jstring JNICALL
          Java_net_surina_soundtouch_SoundTouch_getVersionString(JNIEnv* env, jclass clazz) {
              return env->NewStringUTF(SoundTouch::getVersionString());
          }

          extern "C" JNIEXPORT jstring JNICALL
          Java_net_surina_soundtouch_SoundTouch_getErrorString(JNIEnv* env, jclass clazz) {
              return env->NewStringUTF(g_lastError.c_str());
          }
          EOF
          
          echo "soundtouch-jni.cpp created"
      
      - name: Compile SoundTouch Native Libraries
        run: |
          cd "$SOUNDTOUCH_DIR/source/Android-lib"
          
          export PATH=$ANDROID_HOME/ndk/25.1.8937393:$PATH
          
          echo "Starting NDK build..."
          cd jni
          ndk-build clean
          ndk-build -j4
          
          if [ $? -eq 0 ]; then
            echo "Build successful!"
          else
            echo "Build failed!"
            exit 1
          fi
          
          cd ..
          echo "Build output:"
          ls -la libs/
      
      - name: Create SoundTouch.java Wrapper
        run: |
          mkdir -p output/java/net/surina/soundtouch
          
          cat > output/java/net/surina/soundtouch/SoundTouch.java << 'EOF'
          package net.surina.soundtouch;

          public final class SoundTouch {
              private static native long newInstance();
              private native void deleteInstance(long handle);
              private native void setSampleRate(long handle, int sampleRate);
              private native void setChannels(long handle, int channels);
              private native void setTempo(long handle, float tempo);
              private native void setPitchSemiTones(long handle, float pitch);
              private native void setRate(long handle, float rate);
              private native void putSamples(long handle, short[] samples, int numSamples);
              private native int receiveSamples(long handle, short[] buffer, int maxSamples);
              private native int numSamples(long handle);
              private native void clear(long handle);
              private native void flush(long handle);
              public static native String getVersionString();
              public static native String getErrorString();

              static { System.loadLibrary("soundtouch"); }

              private long handle = 0;
              private int mSampleRate = 0;
              private int mChannels = 0;
              private boolean mIsClosed = false;

              public SoundTouch() {
                  handle = newInstance();
                  if (handle == 0) throw new RuntimeException("Failed to create SoundTouch instance");
              }

              public void setSampleRate(int sampleRate) { if (!mIsClosed) { mSampleRate = sampleRate; setSampleRate(handle, sampleRate); } }
              public void setChannels(int channels) { if (!mIsClosed) { mChannels = channels; setChannels(handle, channels); } }
              public void setTempo(float tempo) { if (!mIsClosed) setTempo(handle, tempo); }
              public void setPitchSemiTones(float pitch) { if (!mIsClosed) setPitchSemiTones(handle, pitch); }
              public void setRate(float rate) { if (!mIsClosed) setRate(handle, rate); }
              public void putSamples(short[] samples, int numSamples) { if (!mIsClosed) putSamples(handle, samples, numSamples); }
              public int receiveSamples(short[] buffer, int maxSamples) { return mIsClosed ? 0 : receiveSamples(handle, buffer, maxSamples); }
              public int numSamples() { return mIsClosed ? 0 : numSamples(handle); }
              public void clear() { if (!mIsClosed) clear(handle); }
              public void flush() { if (!mIsClosed) flush(handle); }
              public int getSampleRate() { return mSampleRate; }
              public int getChannels() { return mChannels; }
              public void close() { if (!mIsClosed && handle != 0) { deleteInstance(handle); handle = 0; mIsClosed = true; } }
              public static boolean isAvailable() { try { return getVersionString() != null; } catch (UnsatisfiedLinkError e) { return false; } }
          }
          EOF
          
          echo "SoundTouch.java created"
      
      - name: Package Artifacts
        run: |
          mkdir -p output/jniLibs
          
          for abi in armeabi-v7a arm64-v8a x86 x86_64; do
            SOURCE="soundtouch/source/Android-lib/libs/$abi/libsoundtouch.so"
            if [ -f "$SOURCE" ]; then
              mkdir -p "output/jniLibs/$abi"
              cp "$SOURCE" "output/jniLibs/$abi/"
              echo "Copied $abi/libsoundtouch.so"
            fi
          done
          
          echo "Final artifacts:"
          find output -type f
      
      - name: Upload SoundTouch Libraries
        uses: actions/upload-artifact@v6
        with:
          name: soundtouch-${{ inputs.soundtouch_version }}-android-libs
          path: output/
          retention-days: 90
